[
  {"file": "server/api.py",
   "name": "the post ignores the text it was sent",
   "find": "    sent = ((body.text if body else None) or \"\").strip()",
   "replace": "    sent = \"\""},

  {"file": "server/api.py",
   "name": "posting does not save what it posted",
   "find": "        d.slack_thread_override = sent",
   "replace": "        pass"},

  {"file": "server/api.py",
   "name": "an empty text wipes the saved post",
   "find": "    text = sent or (d.slack_thread_override or \"\").strip() or format_rca_slack(r, d)",
   "replace": "    text = (d.slack_thread_override or \"\").strip() or format_rca_slack(r, d)\n    sent = sent or ' '"},

  {"file": "client/index.html",
   "name": "an empty section is read back as a deselection",
   "find": "      if (!body) continue;                 // absent because empty, not chosen",
   "replace": "      ;"}
]
