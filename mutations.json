[
  {
    "file": "server/prompts.py",
    "name": "6b loses the data-gap guard",
    "find": "   THE SAME EVIDENCE RULES APPLY as rule 6.",
    "replace": "   Evidence."
  },
  {
    "file": "server/prompts.py",
    "name": "the deviation list stops being exhaustive",
    "find": "Anything outside those three is not a deviation\n      — it is a different outcome from the one you would have chosen.",
    "replace": "Use judgement for anything else."
  },
  {
    "file": "server/prompts.py",
    "name": "the social media fork is left unresolved",
    "find": "so\n        the social-media variant always applies. Do not treat it as unresolved.",
    "replace": "so consider it."
  },
  {
    "file": "server/prompts.py",
    "name": "the booking thread is allowed into support notes",
    "find": "      * the BOOKING THREAD — Zendesk classifies it as a task, not a conversation.",
    "replace": "      * Booking threads."
  },
  {
    "file": "server/prompts.py",
    "name": "internally raised tickets are allowed in",
    "find": "      * INTERNALLY RAISED TICKETS — a DSS follow-up, a bot ping, an ops-to-ops ticket.",
    "replace": "      * Internal tickets."
  },
  {
    "file": "server/prompts.py",
    "name": "Skylar stops being identified as a bot",
    "find": "SKYLAR IS AN AI BOT, not an agent: where Skylar\n                       answered, say so",
    "replace": "note who answered"
  },
  {
    "file": "server/prompts.py",
    "name": "the wait time may be estimated",
    "find": "Read it off the timestamps; do not estimate.",
    "replace": "Give a figure."
  },
  {
    "file": "server/prompts.py",
    "name": "an undetectable field gets guessed",
    "find": "    IF YOU CANNOT DETERMINE A FIELD, LEAVE IT NULL AND WRITE NOTHING FOR IT.",
    "replace": "    Fill in what you can."
  },
  {
    "file": "server/prompts.py",
    "name": "the no-contact sentence changes",
    "find": "\"No direct interaction found between the customer and the support team.\"",
    "replace": "\"No contact found.\""
  },
  {
    "file": "server/services/bigquery_patch.py",
    "name": "the booking timestamp is truncated again",
    "find": "  b.created_at            AS date_of_booking,",
    "replace": "  DATE(b.created_at)      AS date_of_booking,"
  },
  {
    "file": "server/services/rca_v4_validate.py",
    "name": "the projection goes back to dropping the narrative",
    "find": "CONTACT_FIELDS = (\"zd_ref\", \"summary\", \"detail\", \"ce_miss\",\n                  \"time\", \"channel\", \"guest_said\", \"we_said\",\n                  \"wait_for_human\", \"guest_replied\", \"outcome\")",
    "replace": "CONTACT_FIELDS = (\"zd_ref\", \"summary\", \"detail\", \"ce_miss\")"
  },
  {
    "file": "server/services/rca_v4_validate.py",
    "name": "wait_for_human alone is dropped",
    "find": "\"wait_for_human\", \"guest_replied\", \"outcome\")",
    "replace": "\"guest_replied\", \"outcome\")"
  },
  {
    "file": "server/services/rca_v4_validate.py",
    "name": "time and channel are struck again",
    "find": "\"time\", \"channel\", \"guest_said\", \"we_said\",",
    "replace": "\"guest_said\", \"we_said\","
  },
  {
    "file": "server/services/rca_v4_validate.py",
    "name": "the channel vocabulary stops being closed",
    "find": "enums={\"channel\": (CONTACT_CHANNELS, None)})",
    "replace": ")"
  },
  {
    "file": "server/services/rca_v4_validate.py",
    "name": "an invented channel falls back to a token instead of nothing",
    "find": "enums={\"channel\": (CONTACT_CHANNELS, None)})",
    "replace": "enums={\"channel\": (CONTACT_CHANNELS, \"chat\")})"
  },
  {
    "file": "server/services/rca_v4_validate.py",
    "name": "the nothing-found guard stops firing",
    "find": "if not re.search(r\"\\bno (guest )?contact\\b|never (reached|contacted)\",\n                             (r.get(\"summary\") or \"\"), re.I)",
    "replace": "if True"
  },
  {
    "file": "client/index.html",
    "name": "the narrative is not drawn on a matched contact",
    "find": "          ${_narrative(row.note, nIdx)}\n",
    "replace": "          "
  },
  {
    "file": "client/index.html",
    "name": "the narrative is not drawn on an unmatched contact",
    "find": "            ${_narrative(note, oIdx)}\n",
    "replace": "            "
  },
  {
    "file": "client/index.html",
    "name": "empty narrative fields draw an empty bullet",
    "find": "      .filter(k => String(note[k] == null ? '' : note[k]).trim())",
    "replace": "      .filter(() => true)"
  },
  {
    "file": "client/index.html",
    "name": "the summary is drawn as dead text, not editable",
    "find": "        nIdx >= 0 ? edSpan(`support_interaction_notes.${nIdx}.${k}`, note[k])",
    "replace": "        false ? edSpan(`support_interaction_notes.${nIdx}.${k}`, note[k])"
  },
  {
    "file": "client/index.html",
    "name": "an unmatched contact goes back to a dash for its time",
    "find": "      const oTime = String(note.time || '').trim();",
    "replace": "      const oTime = '';"
  },
  {
    "file": "client/index.html",
    "name": "an unmatched contact loses its channel pill",
    "find": "      const oCh = String(note.channel || '').trim();",
    "replace": "      const oCh = '';"
  },
  {
    "file": "client/index.html",
    "name": "the model's time overrides the ticket's",
    "find": "    const tm = (first.time || '').trim() || String((row.note || {}).time || '').trim();",
    "replace": "    const tm = String((row.note || {}).time || '').trim() || (first.time || '').trim();"
  },
  {
    "file": "client/index.html",
    "name": "the model's channel overrides the ticket's",
    "find": "    const ch = (first.thread || '').trim() || String((row.note || {}).channel || '').trim();",
    "replace": "    const ch = String((row.note || {}).channel || '').trim() || (first.thread || '').trim();"
  },
  {
    "file": "server/prompts.py",
    "name": "the time/channel precedence rule is dropped",
    "find": "    `time` AND `channel` ARE READ OFF THE TICKET, NOT JUDGED.",
    "replace": "    State time and channel."
  },
  {
    "file": "server/prompts.py",
    "name": "the model is no longer told the frame wins",
    "find": "the dashboard shows the FRAME's time and channel, not yours",
    "replace": "use your judgement"
  },
  {
    "file": "client/index.html",
    "name": "the summary goes back to a labelled form",
    "find": "      .map(k => `<li class=\"convo-bullet\">${",
    "replace": "      .map(k => `<li class=\"convo-bullet\"><span class=\"k\">${k}</span>${"
  },
  {
    "file": "client/index.html",
    "name": "the bullets collapse into one line",
    "find": "    return lines.length\n      ? `<ul class=\"convo-narr\">${lines.join('')}</ul>` : '';",
    "replace": "    return lines.length\n      ? `<ul class=\"convo-narr\"><li class=\"convo-bullet\">${lines.join(' ')}</li></ul>` : '';"
  },
  {
    "file": "client/index.html",
    "name": "the bullets come out in a different order",
    "find": "  const _NARR = ['guest_said', 'we_said', 'wait_for_human', 'guest_replied',\n                 'outcome'];",
    "replace": "  const _NARR = ['outcome', 'guest_replied', 'wait_for_human', 'we_said',\n                 'guest_said'];"
  },
  {
    "file": "server/prompts.py",
    "name": "the model may answer with fragments instead of sentences",
    "find": "    THESE ARE BULLETS, NOT FORM ANSWERS.",
    "replace": "    Answer each field."
  },
  {
    "file": "server/prompts.py",
    "name": "the worked example of a sentence vs a fragment is dropped",
    "find": "\"Waited 18 minutes before an agent picked it up\", not \"18\n    minutes\".",
    "replace": "Be complete."
  }
]