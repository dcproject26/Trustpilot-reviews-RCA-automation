__pycache__/
*.pyc
.venv/

# Local SQLite state. Each box has its own reviews/drafts; committing one
# would overwrite another machine's data on pull, and the journal/WAL
# siblings churn on every write.
local.db
local.db-*
*.sqlite
*.sqlite-*
