[
  {
    "file": "server/pipeline.py",
    "name": "shortlist: the warehouse is not asked at all (the reported blank card)",
    "find": "            bq = lookup(bid) if bid else None\n            status = \"found\" if bq else \"absent\"",
    "replace": "            bq = None\n            status = \"absent\""
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: a lookup that RAISED is reported as a booking we do not have",
    "find": "        except Exception:\n            bq, status = None, \"failed\"",
    "replace": "        except Exception:\n            bq, status = None, \"absent\""
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: an empty warehouse field blanks out the ticket's value",
    "find": "                if val not in (None, \"\"):\n                    merged[key] = val",
    "replace": "                merged[key] = val"
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: the ticket overwrites the booking's own record",
    "find": "            base = merged",
    "replace": "            base = dict(merged, **{k: v for k, v in base.items() if v})"
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: the Zendesk guest name is dropped, so the hash fallback cannot fire",
    "find": "        base[\"zendesk_guest_name\"] = zd_name",
    "replace": "        base[\"zendesk_guest_name\"] = \"\""
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: a booking the warehouse lacks is dropped from the picker",
    "find": "        tally[status] += 1\n        rows.append(base)",
    "replace": "        tally[status] += 1\n        if status == \"found\":\n            rows.append(base)"
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: an empty booking id is still sent to the warehouse",
    "find": "            bq = lookup(bid) if bid else None",
    "replace": "            bq = lookup(bid)"
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist trail: a healthy shortlist prints a 0-count line as noise",
    "find": "    if not total or not (absent or failed):\n        return None",
    "replace": "    if not total:\n        return None"
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist trail: bookings that could not be looked up go unmentioned",
    "find": "    if failed:\n        bits.append(f\"{failed} could not be looked up, so nothing about \"",
    "replace": "    if False:\n        bits.append(f\"{failed} could not be looked up, so nothing about \""
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist trail: missing bookings go unmentioned",
    "find": "    if absent:\n        bits.append(f\"{absent} not in the warehouse at all",
    "replace": "    if False:\n        bits.append(f\"{absent} not in the warehouse at all"
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: the lookup answer never reaches the candidate the picker gets",
    "find": "                            _c[\"details_lookup\"] = _row[\"details_lookup\"]",
    "replace": "                            pass"
  },
  {
    "file": "server/pipeline.py",
    "name": "shortlist: the Zendesk name is not copied onto the candidate",
    "find": "                            _c[\"zendesk_guest_name\"] = _row[\"zendesk_guest_name\"]",
    "replace": "                            pass"
  },
  {
    "file": "client/index.html",
    "name": "sent: the sent bucket falls through to the Tier 1 vocabulary again",
    "find": "  if (bucket === 'sent') {\n    if (!_matchBucket(row, tier, candState).startsWith('identified'))\n      return 'Sent without a confirmed booking';\n    bucket = 'identified';\n  }",
    "replace": ""
  },
  {
    "file": "client/index.html",
    "name": "sent: an unmatched sent review is called identified",
    "find": "    if (!_matchBucket(row, tier, candState).startsWith('identified'))\n      return 'Sent without a confirmed booking';",
    "replace": ""
  },
  {
    "file": "client/index.html",
    "name": "sent: a MATCHED sent review loses its title too",
    "find": "    bucket = 'identified';",
    "replace": "    return 'Sent without a confirmed booking';"
  },
  {
    "file": "client/index.html",
    "name": "sent: the tab stops filing sent reviews under sent",
    "find": "  if (row.status === 'sent') return 'sent';\n  return _matchBucket(row, tier, candState);",
    "replace": "  return _matchBucket(row, tier, candState);"
  },
  {
    "file": "client/index.html",
    "name": "confirm: the booking-details panel stops saying the record was unread",
    "find": "      ${b.detailsLookup && b.detailsLookup !== 'found' ? `",
    "replace": "      ${false ? `"
  },
  {
    "file": "client/index.html",
    "name": "confirm: details_lookup is dropped by the booking remap",
    "find": "        r.booking.detailsLookup    = db.details_lookup                    || r.booking.detailsLookup;",
    "replace": ""
  },
  {
    "file": "server/api.py",
    "name": "confirm: a failed lookup is reported as a missing booking",
    "find": "        lookup, why = \"failed\", f\"the lookup did not complete ({type(e).__name__})\"",
    "replace": "        lookup, why = \"absent\", \"the warehouse has no booking with this id\""
  },
  {
    "file": "server/api.py",
    "name": "confirm: nothing is recorded on the draft, only in the log",
    "find": "    d.booking[\"details_lookup\"] = lookup",
    "replace": "    pass"
  },
  {
    "file": "server/api.py",
    "name": "confirm: the trail line is not written",
    "find": "        d.confidence_trail = _trail\n        flag_modified(d, \"confidence_trail\")",
    "replace": "        pass"
  },
  {
    "file": "server/api.py",
    "name": "confirm: the stored booking aliases the candidates_list entry again",
    "find": "if full else dict(match)",
    "replace": "if full else match"
  }
]